extends ../../layout

block content
    .container
        .jumbotron
            h1 Voici la #{title}
            table.table.table-condensed.table-responsive
                thead
                    tr
                        th Titre
                        th Detail
                tbody
                    each livre in listLivre
                        tr
                            td= livre.titre
                            td
                                a(href='/catalogue/livre/livreDetail/' + livre.idDocument)    Afficher detaill